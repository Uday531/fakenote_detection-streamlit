This project uses streamlit api to create the interface for the users to take a picture of the note or just simply upload it and test it to determine which is real note and which is false
i have not given the datasets to train because git not accessing it
